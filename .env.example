DATABASE_URL=postgresql://openenrich:openenrich@localhost:5432/openenrich
REDIS_URL=redis://localhost:6379/0

# BYOK vault — generate with: python -c "import secrets; print(secrets.token_hex(32))"
ENCRYPTION_MASTER_KEY=

# Default LLM (fallback when no BYOK key in vault)
DEFAULT_LLM_PROVIDER=openai
DEFAULT_LLM_API_KEY=sk-your-key-here
DEFAULT_LLM_MODEL=gpt-4o-mini

# Optional
PROXY_URLS=
WEBHOOK_URL=
